<?xml version="1.0" encoding="ISO-8859-1"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
	<parent>
		<artifactId>layers</artifactId>
		<groupId>no.stelvio.layers</groupId>
		<version>1.0.0-SNAPSHOT</version>
	</parent>
	<artifactId>stelvio-presentation-star-example-java</artifactId>
	<groupId>no.stelvio.star.example</groupId>
	<packaging>jar</packaging>
	<version>1.0.0-SNAPSHOT</version>
	<name>star-example :: presentation - common</name>
	<url>${basedir}/target/site/</url>

	<dependencies>

    </dependencies>
    <build>
        <!-- TODO
          Have these int.test specific settings in the top parent pom, but inside the plugin management section,
          see http://maven.apache.org/pom.html under 'Plugin Management'. Maybe alter the life cycle for these
          poms to include integration test into the default life cycle (look into extensions).
        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>testCompile</goal>
                        </goals>
                        <configuration>
                            <compileSourceRoots>
                                <compileSourceRoot>${basedir}/src/it/java</compileSourceRoot>
                            </compileSourceRoots>
                            <outputDirectory>${project.build.directory}/it</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <executions>
                    <execution>
                        <id>it</id>
                        <phase>integration-test</phase>
                        <goals>
                            <goal>test</goal>
                        </goals>
                        <configuration>
                            <testClassesDirectory>${project.build.directory}/it</testClassesDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
         -->
    </build>
</project>
