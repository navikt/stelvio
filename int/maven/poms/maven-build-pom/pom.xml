<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	
	<parent>
		<groupId>no.stelvio.maven.poms</groupId>
		<artifactId>maven-root-pom</artifactId>
		<version>1.2.1</version>
		<relativePath>../maven-root-pom</relativePath>
	</parent>
	
	<groupId>no.nav.maven.poms</groupId>
	<artifactId>maven-build-pom</artifactId>
	<packaging>pom</packaging>
	<version>1.0-SNAPSHOT</version>
	<name>maven-build-pom</name>

	<properties>
		<buildVersion>1.0-SNAPSHOT</buildVersion>
	</properties>

	<dependencies>

	</dependencies>

	<build>
	<!--
	  Build process:
	  1. Update views - initialize
	  2. Get activities (DEV) - validate
	  3. Remove SNAPSHOT (INT) - 
	  4. Deliver I (DEV -> INT) - generate-resources
	  5. COMPILE
	  6. Update maven repo
	  7. Put back SNAPSHOT
	  8. Deliver II (DEV -> INT) - package
	  9. Create baseline - package
	  10. Update Poms
	  11. Update CQ activities	  
	-->
		<plugins>
			<plugin>
				<groupId>no.stelvio.maven.plugins</groupId>
				<artifactId>maven-build-plugin</artifactId>
				<version>${buildVersion}</version>
				<executions>
					<!-- Step 2: Remove WARN_UNRESERVED trigger -->
					<execution>
						<id>Remove WARN_UNRESERVED trigger</id>
						<phase>validate</phase>
						<goals>
							<goal>editWarnUnreserved</goal>
						</goals>
						<configuration>
							<action>remove</action>
						</configuration>
					</execution>
				</executions>
			</plugin>
			
			<plugin>
				<groupId>no.stelvio.maven.plugins</groupId>
				<artifactId>maven-build-plugin</artifactId>
				<version>${buildVersion}</version>
				<executions>
					<!-- Step 1: update views -->
					<execution>
						<id>updateViews</id>
						<phase>initialize</phase>
						<goals>
							<goal>updateViews</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			
			<plugin>
				<groupId>no.stelvio.maven.plugins</groupId>
				<artifactId>maven-build-plugin</artifactId>
				<version>${buildVersion}</version>
				<executions>
					<!-- Step 3: deliver I -->
					<execution>
						<id>deliverCandidates</id>
						<phase>initialize</phase>
						<goals>
							<goal>deliverCandidates</goal>
						</goals>
						<configuration>
							<step>1</step>
						</configuration>
					</execution>
				</executions>
			</plugin>
			
			<plugin>
				<groupId>no.stelvio.maven.plugins</groupId>
				<artifactId>maven-build-plugin</artifactId>
				<version>${buildVersion}</version>
				<executions>
					<execution>
						<id>deliverCandidates</id>
						<phase>install</phase>
						<goals>
							<goal>undoDelivery</goal>
						</goals>
					</execution>
				</executions>
			</plugin> 
			
			<plugin>
				<groupId>no.stelvio.maven.plugins</groupId>
				<artifactId>maven-build-plugin</artifactId>
				<version>${buildVersion}</version>
				<executions>
					<!-- Step 4: Add WARN_UNRESERVED trigger -->
					<execution>
						<id>Add WARN_UNRESERVED trigger</id>
						<phase>install</phase>
						<goals>
							<goal>editWarnUnreserved</goal>
						</goals>
						<configuration>
							<action>add</action>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<!-- <plugin>
				<groupId>no.stelvio.maven.plugins</groupId>
				<artifactId>maven-build-plugin</artifactId>
				<version>${buildVersion}</version>
				<executions>
					<execution>
						<id>deliverCandidates</id>
						<phase>package</phase>
						<goals>
							<goal>deliverCandidates</goal>
						</goals>
						<configuration>
							<step>2</step>
						</configuration>
					</execution>
				</executions>
			</plugin> -->
			
			<!-- <plugin>
				<groupId>no.stelvio.maven.plugins</groupId>
				<artifactId>maven-build-plugin</artifactId>
				<version>${buildVersion}</version>
				<executions>
					<execution>
						<id>deliverCandidates</id>
						<phase>package</phase>
						<goals>
							<goal>baseline</goal>
						</goals>
					</execution>
				</executions>
			</plugin> -->
			
		</plugins>
	</build>
</project>
