<?xml version="1.0" encoding="ISO-8859-1"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>no.stelvio.maven.poms</groupId>
		<artifactId>maven-plugin-pom</artifactId>
		<version>1.2</version>
		<relativePath>../../poms/maven-plugin-pom</relativePath>
	</parent>

	<groupId>no.nav.maven.plugins</groupId>
	<artifactId>maven-enforcer-plugin</artifactId>
	<packaging>maven-plugin</packaging>
	<version>1.0-SNAPSHOT</version>
	<name>Stelvio Integration Framework Enforcer Plugin</name>

	<!--
		TODO: This plugin only exists because of Maven bug MNG-1323: Plugin extensions (dependencies) not resilved in reactor builds.
		We want to add custom rules by adding dependencies to the standard maven-enforcer-plugin, but this doesn't work on Maven 2.x
		because of this bug. This bug is supposedly fixed in Maven 3.x, and this plugin should be removed when migration to Maven 3.x
		is done.
	-->
	
	<build>
		<plugins>
			<plugin>
				<groupId>org.ops4j</groupId>
				<artifactId>maven-inherit-plugin</artifactId>
				<version>1.1</version>
				<executions>
					<execution>
						<goals>
							<goal>inherit</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<dependencies>
		<!-- Had to add this dependency to avoid link error -->
		<dependency>
			<groupId>org.codehaus.plexus</groupId>
			<artifactId>plexus-utils</artifactId>
		</dependency>

		<dependency>
			<groupId>org.apache.maven.plugins</groupId>
			<artifactId>maven-enforcer-plugin</artifactId>
			<type>maven-plugin</type>
			<version>${enforcer.plugin.version}</version>
		</dependency>

		<dependency>
			<groupId>no.stelvio.maven.support</groupId>
			<artifactId>custom-enforcer-rules</artifactId>
			<version>1.0</version>
		</dependency>
	</dependencies>

	<scm>
		<connection>${scm.connection}</connection>
		<developerConnection>${scm.connection}</developerConnection>
	</scm>
	<properties>
		<scm.root>
			scm:svn:https://versjonskontroll.adeo.no/svn/stelvio/trunk/int/maven/plugins/maven-enforcer-plugin
		</scm.root>
		<scm.connection>${scm.root}</scm.connection>
	</properties>
</project>