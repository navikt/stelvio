<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>no.stelvio.example</groupId>
	<artifactId>stelvio-example-echo-service-jaxws</artifactId>
	<version>1.0-SNAPSHOT</version>
	<name>Stelvio Example ${project.artifactId}</name>
	<packaging>nav-war</packaging> 
	<build>
		<plugins>
			<plugin>
				<groupId>no.nav.maven.plugins</groupId>
				<artifactId>maven-wsdl-plugin</artifactId>
				<version>1.11-SNAPSHOT</version>
				<executions>
					<execution>
						<goals>
							<goal>wsimport</goal>
						</goals>
						<configuration>
							<wsdlOptions>
								<wsdlOption>
									<wsdlArtifact>
										<groupId>no.stelvio.example</groupId>
										<artifactId>stelvio-example-echo-service</artifactId>
										<version>1.0-SNAPSHOT</version>
										<type>wsdl-interface</type>
									</wsdlArtifact>
								</wsdlOption>
							</wsdlOptions>
						</configuration>
					</execution>
				</executions>
			</plugin>
			
			<!-- Delete the files in the WebContent/WEB-INF/lib and WebContent/META-INF folder -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-clean-plugin</artifactId>
				<version>2.4.1</version><configuration>
					<filesets>
						<fileset>
							<directory>
								${basedir}/WebContent/WEB-INF/lib/
							</directory>
						</fileset>
						<fileset>
							<directory>
								${basedir}/WebContent/META-INF/
							</directory>
						</fileset>
					</filesets>
				</configuration>
			</plugin>

			<!-- Create the WAR file -->
			<plugin>
				<groupId>no.nav.maven.plugins</groupId>
				<artifactId>nav-war-plugin</artifactId>
				<version>1.3</version><!--  EHRM TODO -->
				<extensions>true</extensions>
				<configuration>
					<warSourceExcludes>
						WEB-INF/lib/*.jar, **/*.db
					</warSourceExcludes>

					<archive>
						<manifest>
							<classpathPrefix>lib/</classpathPrefix>
							<addClasspath>true</addClasspath>
						</manifest>
					</archive>

					<!-- Export generated manifest for use with IDE -->
					<exportManifestToDir>
						${basedir}/WebContent/META-INF/
					</exportManifestToDir>

					<warSourceDirectory>
						${basedir}/WebContent
					</warSourceDirectory>
					<webXml>
						${basedir}/WebContent/WEB-INF/web.xml
					</webXml>
				</configuration>
			</plugin>
									
		</plugins>
	</build>
</project>