<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>no.stelvio.example</groupId>
	<artifactId>stelvio-example-echo-test</artifactId>
	<version>1.0.0-SNAPSHOT</version>

	<dependencies>
		<dependency>
			<groupId>no.stelvio.example</groupId>
			<artifactId>stelvio-example-echo-consumerEAR</artifactId>
			<type>ear</type>
			<version>1.0.0-SNAPSHOT</version>
		</dependency>
		<dependency>
			<groupId>no.stelvio.example</groupId>
			<artifactId>stelvio-example-echo-providerEAR</artifactId>
			<type>ear</type>
			<version>1.0.0-SNAPSHOT</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>2.2</version>
				<executions>
					<execution>
						<goals>
							<goal>copy-dependencies</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>was6-maven-plugin</artifactId>
				<version>1.1.1</version>
				<executions>
					<execution>
						<id>provider-deploy</id>
						<phase>pre-integration-test</phase>
						<goals>
							<goal>installApp</goal>
						</goals>
						<configuration>
							<earFile>${project.build.directory}/dependency/stelvio-example-echo-providerEAR-1.0.0-SNAPSHOT.ear</earFile>
							<applicationName>stelvio-example-echo-providerEAR</applicationName>
						</configuration>
					</execution>
					<!-- does not work -- must debug later
					<execution>
						<id>consumer-deploy</id>
						<phase>pre-integration-test</phase>
						<goals>
							<goal>installApp</goal>
						</goals>
						<configuration>
							<earFile>${project.build.directory}/dependency/stelvio-example-echo-consumerEAR-1.0.0-SNAPSHOT.ear</earFile>
							<applicationName>stelvio-example-echo-consumerEAR</applicationName>
						</configuration>
					</execution>
					-->
				</executions>
				<configuration>
					<wasHome>${wid.runtime}</wasHome>
					<conntype>SOAP</conntype>
					<host>e26apvl036.test.local</host>
					<port>8879</port>
					<username>srvPensjon</username>
					<password>Ash5SoxP</password>
					<targetCluster>SS_U2.AppTarget</targetCluster>
					<updateExisting>true</updateExisting>
				</configuration>
			</plugin>
			
			<plugin>
				<groupId>eviware</groupId>
				<artifactId>maven-soapui-plugin</artifactId>
				<version>3.6.1</version>
				<executions>
					<execution>
						<phase>integration-test</phase>
						<goals>
							<goal>test</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<outputFolder>${project.build.directory}/soapui/</outputFolder>
					<projectFile>${basedir}/${project.artifactId}-soapui-project.xml</projectFile>
					<testSuite>Smoke</testSuite>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>