<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>no.nav.maven.poms</groupId>
		<artifactId>maven-integration-root-pom</artifactId>
		<version>1.1</version>
	</parent>

	<groupId>no.nav.maven.poc</groupId>
	<artifactId>layers</artifactId>
	<version>1.7-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>Maven POC ${artifactId}</name>

	<modules>
		<module>libs</module>
		<module>modules</module>
	</modules>

	<dependencies>
		<dependency>
			<groupId>com.ibm.maven.runtimes</groupId>
			<artifactId>wps-runtime</artifactId>
			<version>6.1</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>no.stelvio.int</groupId>
			<artifactId>stelvio-commons-lib</artifactId>
			<version>3.1.4</version>
		</dependency>
	</dependencies>

	
	<build>
		<directory>${target.dir}</directory>
		<outputDirectory>${target.dir}/classes</outputDirectory>
		<testOutputDirectory>
			${target.dir}/test-classes
		</testOutputDirectory>
		<sourceDirectory />
		<resources>
			<resource>
				<!-- TODO: This is here because WID insists on having the root folder as a source folder -->
				<!-- TODO: Review which files to exclude/include -->
				<directory>${basedir}</directory>
				<excludes>
					<exclude>.settings/**</exclude>
					<exclude>bin/**</exclude>
					<exclude>gen/**</exclude>
				</excludes>
				<includes>
					<include>**/*.wsdl</include>
					<include>**/*.xsd</include>
					<include>**/*.module</include>
					<include>**/*.modulex</include>
					<include>**/*.component</include>
					<include>**/*.import</include>
					<include>**/*.export</include>
					<include>**/*.ifm</include>
					<include>**/*.map</include>
					<include>**/*.medflow</include>
					<include>**/*.mfc</include>
					<include>**/*.xsl</include>
					<include>**/*.bpel</include>
					<include>**/*.bpelex</include>
					<include>**/*.mon</include>
				</includes>
			</resource>
		</resources>
		<pluginManagement>
			<plugins>
				<plugin>
					<artifactId>maven-compiler-plugin</artifactId>
					<configuration>
						<verbose>true</verbose>
						<fork>true</fork>
						<executable>
							${wid.runtime}\java\bin\javac
						</executable>
						<compilerVersion>1.5</compilerVersion>
					</configuration>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<groupId>no.nav.maven.plugins</groupId>
				<artifactId>maven-wps-artifact-plugin</artifactId>
				<extensions>true</extensions>
			</plugin>
		</plugins>
	</build>

	<properties>
		<!-- 
			TODO: WID goes bananas if the Maven build is using the default target folder as output. 
			As a workaround we are changing the target folder to a folder outside the project		
		-->
		<!-- TODO: Review this setting -->
		<target.dir>
			/data/maven-builds/maven-target/${groupId}/${artifactId}/${version}
		</target.dir>
	</properties>

	<!-- Added because parent pom is not the parent directory in SCM -->
	<scm>
		<connection>
			scm:svn:https://versjonskontroll.adeo.no/svn/stelvio/trunk/incubator/maven/maven-poc/layers
		</connection>
	</scm>
</project>
