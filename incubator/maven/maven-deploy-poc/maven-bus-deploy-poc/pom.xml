<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>no.nav.maven.poms</groupId>
        <artifactId>maven-deploy-pom</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <groupId>no.nav.maven.poc</groupId>
    <artifactId>poc-deploy</artifactId>
    <packaging>deployable-artifact</packaging>
    <version>1.0-SNAPSHOT</version>
    <name>poc-deploy</name>
    <url>http://maven.apache.org</url>

	<!-- TODO: This plugin downstairs will be injected otherwise -->
	<build>
		<plugins>
               <plugin>
				<groupId>nav.maven.plugins</groupId>
				<artifactId>maven-propertiesgenerator-plugin</artifactId>
                <configuration>
					<templateDir>${basedir}/src/main/scripts/templates</templateDir>
					<environmentName>${miljo}</environmentName>
					<outputDir>${basedir}/src/main/scripts/app_props</outputDir>
					<environmentDir>${basedir}/src/main/scripts/environments</environmentDir>
				</configuration>
				<executions>
					<execution>
						<phase>install</phase>
						<goals>
							<goal>generate</goal>
						</goals>
					</execution>
               	</executions>
           	</plugin>
		</plugins>
	</build>
    <dependencies>
        <dependency>
            <groupId>no.nav.maven.poc</groupId>
            <artifactId>bus-deploy</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>

        <dependency>
            <groupId>no.nav.maven.support</groupId>
            <artifactId>busconfiguration</artifactId>
            <version>1.0-SNAPSHOT</version>
        </dependency>

    </dependencies>
</project>
