package no.stelvio.common.codestable;

import org.junit.Test;

/**
 * Unit test for {@link CodesTableNotFoundException}.
 *
 * @author personf8e9850ed756
 */
public class CodesTableNotFoundExceptionTest {
    
	/**
	 * The purpose of message templates is fulfilled through a new Logger
	 * component, message template usage is deprecated. Hence test is commented
	 * out (to be deleted).
	 */
	@Test
    public void messageIsCorrect() {
//        CodesTableNotFoundException ctnf = new CodesTableNotFoundException(TestCti.class);
//
//	    MatcherAssert.assertThat(ctnf.getMessage(), containsString(TestCti.class.toString()));
//	    MatcherAssert.assertThat(ctnf.getMessage(), endsWith("does not exist"));
//	    MatcherAssert.assertThat(ctnf.getMessage(), startsWith("Codestable"));
    }
}
